// Defina um array de exemplo
var meuArray = [1, 2, 3, 4, 5];

// Crie um loop for que começa do último elemento até o primeiro
for (var i = meuArray.length - 1; i >= 0; i--) {
  console.log(meuArray[i]);
}